<div class="welcome-message">
  <h2> You make a living from what you get, <br>
    but you make a life from what you give! </h2>
    <h1> GIFTe</h1>
</div>

<div id="form-signup"
  <div class="row">
    <div class="col-xs-4 col-xs-offset-4 ">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.label :first_name, class: "sr-only"%> <!-- screen reader only -->
          <%= f.text_field :first_name, placeholder: "first name", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :last_name, class: "sr-only"  %>
          <%= f.text_field :last_name, placeholder: "last name", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :email, class: "sr-only"%>
          <%= f.email_field :email, autofocus: true, placeholder: "email", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :location, class: "sr-only" %>
          <%= f.text_field :location, placeholder: "city", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :password, class: "sr-only" %> 
            <% if @validatable %><i>(<%= @minimum_password_length %> characters minimum)</i><% end %>
          <%= f.password_field :password, autocomplete: "off", placeholder: "password (minimum 8 characters)", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation, class: "sr-only" %>
          <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "confirmed password", class: "form-control" %>
        </div>
          
        <%= f.submit "Sign up", class: 'btn btn-info' %> 
      <% end %>
    </div>
  </div>
</div>