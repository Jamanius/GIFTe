<%= form_for(@gift) do |f| %>
  <% if @gift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gift.errors.count, "error") %> prohibited this gift from being saved:</h2>

      <ul>
      <% @gift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field, form-group">
    <%= f.label :title, class: "sr-only" %><br>
    <%= f.text_field :title, placeholder: "Title", class: "form-control"  %>
  </div>

  <div class="field, form-group">
    <%= f.label :description, class: "sr-only" %><br>
    <%= f.text_area :description, placeholder: "Description", class: "form-control"  %>
  </div>
  <div class="field">
    <%= f.label :gifts_type, class: "sr-only" %><br>
    <%= f.text_field :gifts_type, placeholder: "Is it eadible, wearable, useable or companionable", class: "form-control"  %>
  </div>
  <div class="field">
    <%= f.label :comments, class: "sr-only" %><br>
    <%= f.text_area :comments, placeholder: "Comments", class: "form-control"  %>
  </div>
  <br>
  <button type="button" class="btn btn-warning">
      Upload an image
  </button>


  <%= f.submit "Submit", class: 'btn btn-info' %><br> 
<br>
<% end %>
